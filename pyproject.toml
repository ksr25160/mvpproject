[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "meeting-ai-assistant"
version = "1.0.0"
description = "Azure-based Meeting AI Assistant"
authors = [{name = "Meeting AI Team"}]
readme = "README.md"
requires-python = ">=3.11"
